[project]
name = "gen-textures"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fonttools>=4.63.0",
]

[project.scripts]
gen-textures = "gen_textures:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
